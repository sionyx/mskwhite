services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mskwhitebot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./payments.db:/app/payments.db
    command: ["python", "bot.py"]
